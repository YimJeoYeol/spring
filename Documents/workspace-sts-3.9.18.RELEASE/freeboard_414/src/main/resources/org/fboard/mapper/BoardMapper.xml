<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="org.fboard.mapper.BoardMapper"> <!-- <![CDATA[쿼리문]]> 비교연산자할때 필요-->
  
  	<select id="getList" resultType="org.fboard.domain.BoardDTO">
  	<![CDATA[
  		select * from tbl_board order by bno desc
  		]]>
  	</select>
  	<select id="getListWithPaging" resultType="org.fboard.domain.BoardDTO">
  	<![CDATA[
		select * from 
	  		(
	  		select /*+ INDEX_DESC(tbl_board pk_board)*/
	  			rownum rn, bno, title, content, writer, regdate,updatedate
	  		from
	  			tbl_board
	  		where rownum <= #{pageNum} * #{amount}
	  		) 
        where rn > (#{pageNum}-1) * #{amount}
  			]]>
  	</select>
  	
  	<select id="searchTitle" parameterType="String" resultType="org.fboard.domain.BoardDTO">
  		select * from tbl_board where title like '%'||#{svalue}||'%'
  	</select>
  	
  	<insert id="insert">
  		insert into tbl_board (bno, title, content, writer) values (seq_board.nextval, #{title}, #{content}, #{writer})
  	</insert>
  	
  	<insert id="insertSelectKey">
  		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
  			select seq_board.nextval from dual
  		</selectKey>
  		insert into tbl_board (bno, title, content, writer) values (#{bno}, #{title}, #{content}, #{writer})
  	</insert>
  	
  	<select id="getOne" resultType="org.fboard.domain.BoardDTO">
  		select * from tbl_board where bno=#{bno}	
  	</select>
  	
  	<delete id="delete">
  		delete from tbl_board where bno=#{bno}
  	</delete>
  	
  	<update id="update">
  		update tbl_board set title=#{title}, content=#{content}, writer=#{writer}, updatedate=sysdate where bno=#{bno}
  	</update>
  	
  	<select id="getTotalCount" resultType="long">
  		select count(*) from tbl_board
  	</select>
  	
  	
  </mapper>
  
  
  
  